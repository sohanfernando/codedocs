sonar.projectKey=sohanfernando_codedocs
sonar.organization=sohanfernando

# One project covering both halves of the monorepo, same as Automatic
# Analysis was already doing.
sonar.sources=backend/src/main,frontend/src
sonar.tests=backend/src/test
sonar.exclusions=**/node_modules/**,**/target/**,**/dist/**,frontend/public/**
sonar.sourceEncoding=UTF-8

# Java: compiled classes + JaCoCo report, so rules that need bytecode
# (not just source) run, and real coverage numbers show up instead of 0%.
sonar.java.binaries=backend/target/classes
sonar.java.test.binaries=backend/target/test-classes
sonar.coverage.jacoco.xmlReportPaths=backend/target/site/jacoco/jacoco.xml

# Dependency jars (Spring, JGit, JUnit, Testcontainers, ...), so the Java
# analyzer can fully resolve types instead of warning about reduced
# precision. ${env.HOME} is the scanner's own placeholder syntax — resolved
# against the real process environment at scan time, not a CI-specific
# hardcoded path — and by the time this runs, the CI workflow's `mvn verify`
# step has already populated ~/.m2/repository with everything needed.
sonar.java.libraries=${env.HOME}/.m2/repository/**/*.jar
sonar.java.test.libraries=${env.HOME}/.m2/repository/**/*.jar
