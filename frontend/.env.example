# Optional — leave unset locally. Sentry DSNs aren't secret (they're baked
# into the client bundle), this is just kept out of the repo so each
# environment (local/staging/prod) can point at its own Sentry project.
VITE_SENTRY_DSN=
